{
    "name": "rust-devcontainer",
    "image": "mcr.microsoft.com/devcontainers/rust:latest",
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y neovim && rustc --version",
    "remoteUser": "vscode"
}
